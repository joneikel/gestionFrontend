import React from 'react';
import VerticalBar from './components/VerticalBar';

const ChartsPage = () => {
    return (
        <div style={{width: '100%'}}>
            <VerticalBar />
        </div>
    );
}

export default ChartsPage;
